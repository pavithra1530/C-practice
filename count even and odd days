#include <stdio.h>
int main() {
    int N;
    scanf("%d", &N);
    int num;
    int evencount=0,oddcount=0;
    for(int i=0;i<N;i++){
        scanf("%d",&num);

        if(num % 2==0)
            evencount++;
        else
            oddcount++;
    }
    printf("even: %d odd: %d", evencount, oddcount);

    return 0;
}