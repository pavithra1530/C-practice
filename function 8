#include <stdio.h>
int reverseNumber(int n)
{
    int rev = 0;
    while(n != 0)
    {
        rev = rev * 10 + n % 10;
        n = n / 10;
    }
    return rev;
}
int main()
{
    int num;
    scanf("%d", &num);
    printf("%d", reverseNumber(num));
    return 0;
}