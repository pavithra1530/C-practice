#include <stdio.h>
int main() {
    int N,reverse =0,remainder=0;
    scanf("%d",&N);

    for (int i= N; i>0; i = i/10) {

        reverse = reverse *10+(i%10);

    }
            printf("%lld ", reverse);


    return 0;
}