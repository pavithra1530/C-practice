#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    if (n>100 ) {
        printf("Three-digit number");
    }
    else if (n>10&&n<999){
        printf("Two digit number");
    }
    else {
        printf("Three-digit number");
    }
    return 0;
}