#include <stdio.h>
int main() {
    int n,i,digit,count=0;
    scanf("%d", &n);
    for (int i=n; i>0; i=i/10) {
        digit=i%10;
        if (digit%2==0) {
            count++;
        }
    }
    printf("%d",count);
    return 0;
}